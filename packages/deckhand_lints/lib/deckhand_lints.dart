/// Placeholder exported symbol so `dart pub get` accepts this package.
/// The actual artifact consumers use is `lib/analysis_options.yaml`,
/// referenced via `include: package:deckhand_lints/analysis_options.yaml`.
library;

// Version string for tooling that wants to surface the active ruleset.
const String deckhandLintsVersion = '0.1.0';
