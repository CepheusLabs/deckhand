/// Deckhand profile-script API. Imported by Dart scripts shipped inside
/// profiles' `scripts/` directories. Runtime sandboxing is enforced by
/// the Deckhand app when it loads and executes a script.
library deckhand_profile_script;

export 'src/api.dart';
